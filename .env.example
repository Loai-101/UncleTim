# Canonical production origin (SEO, sitemap, Open Graph, robots, schema).
# Must match the live domain. Do not use localhost or *.vercel.app here.
NEXT_PUBLIC_SITE_URL=https://uncletim.horse

# Optional Google Search Console HTML tag verification token (server-only).
GOOGLE_SITE_VERIFICATION=
